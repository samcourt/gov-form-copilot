# Type fix patch

Copy these files into your repo root, overwriting existing files.

Then run:

```powershell
npm run build --workspace=@gov-form-copilot/shared
npm run build --workspace=@gov-form-copilot/server
npm run build:extension
```
