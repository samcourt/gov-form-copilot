// Server evidence types are aliases of the shared package types.
// Keep this file only for backwards-compatible imports inside the server.
export type {
  EvidenceSourceType,
  EvidenceStatus,
  EvidenceValue,
  EvidenceDocument,
  ProfileField,
  CanonicalProfile,
  ProfileBuildResult
} from "@gov-form-copilot/shared";
