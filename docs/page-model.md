# Page Model

The content script converts HTML into a semantic model.

Each field contains:

- label
- type
- required
- help text
- validation
- section
- safeToFill
- options

This abstraction allows the AI to reason about intent rather than raw DOM.
